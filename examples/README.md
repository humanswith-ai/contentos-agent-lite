# Example run

An **illustrative** end-to-end run of the gates, so you can see the shapes before you start.
The "facts" below are realistic examples that cite real public doc pages — **replace them with
your own sourced facts** for real use. The `qa_report.md` score is the *actual* output of
`python -m scripts.cli score` on `draft.md`.

Flow: `source_pack.md` → `brief.md` → `draft.md` → `qa_report.md`
(topic: *choosing a payment stack by payout speed, not just fees*).
