# All keys are OPTIONAL. The agent works with zero keys (it does the steps itself).
# Each key makes the deterministic scripts/ layer sharper.

# Research (LLM-native search) — free 1000/mo — https://app.tavily.com
TAVILY_API_KEY=

# Read a specific URL -> clean markdown — free 500 credits — https://firecrawl.dev
FIRECRAWL_API_KEY=

# SERP / People-Also-Ask / related — free 2,500 credits, no card — https://serper.dev
SERPER_API_KEY=

# Optional: standalone-CLI LLM (NOT needed inside Claude Code/Cursor/Codex)
# ANTHROPIC_API_KEY=
